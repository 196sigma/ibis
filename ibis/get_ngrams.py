#!/usr/bin/env python
import nltk
from collections import Counter

def ngrams_text(text, n=2):
    """text: a filepath to a document"""
    ngrams = []
    return ngrams

def ngrams_corpus(corpus, n=2):
    """corpus: a corpus object"""
    ngrams = []
    return ngrams

def ngrams(x, n=2):
    if x is filepath:
        return ngrams_text(x,n)
    elif x is corpus:
        return ngrams_corpus(x, n)
    else:
        pass
